# Development-only tools — the production server never needs these.
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3      # the test runner: `pytest` from the project root
