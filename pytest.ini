# Pytest configuration. `testpaths` means a bare `pytest` from the project
# root finds the suite without being told where to look.
[pytest]
testpaths = tests
# Treat warnings loudly in CI-minded projects; for now keep default output
# readable for a human learning the suite.
addopts = -q
